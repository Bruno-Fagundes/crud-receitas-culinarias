// frontend/postcss.config.js
export default {
  plugins: {
    // Use '@tailwindcss/postcss' como o nome do plugin, conforme a mensagem de erro.
    // O pacote 'tailwindcss' em si ainda é necessário.
    '@tailwindcss/postcss': {},
    autoprefixer: {},
  },
}